shader_type canvas_item;

void vertex()
{
	//float uv = (VERTEX.y - gradient_offset);
	//COLOR.rgb = (COLOR.r * (texture(gradient, vec2(uv, 0.0)).rgb)) + (COLOR.g * 1.0) + (COLOR.b * 0.0);
	COLOR.rgb *= 0.0;
}
